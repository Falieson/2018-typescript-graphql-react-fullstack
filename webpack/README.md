# Webpack Structure

```text
| Webpack-Merge
├── Common
│   ├──  Client (Common)
│         ├──  Dev (Client)
│         ├──  Prod (Client)
│     |--  Server (Common)
│         ├──    Dev (Server)
│         ├──    Prod (Server)
```

## Server

Instead of using webpack's devServer we will use the server created at `/src/server/`
